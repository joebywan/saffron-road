<?xml version="1.0" encoding="utf-8"?>
<!--
    NO <uses-permission android:name="android.permission.INTERNET" />, AND
    THAT IS THE POINT.

    Every byte the page loads comes out of the APK's own assets through
    WebViewAssetLoader, which answers the request before it ever reaches the
    network stack. Without the permission there is no route off the device at
    all: no telemetry, no CDN, no accidental fetch. If a future change to the
    web app adds a real network request it will fail — loudly, in the console
    — rather than quietly working on wifi and breaking on a train.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaffronRoad"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|keyboard|navigation|uiMode|density|fontScale"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
